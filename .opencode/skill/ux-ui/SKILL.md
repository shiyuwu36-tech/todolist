---
name: ux-ui
description: |
  触发场景：需要定义页面布局、组件清单、交互流程、状态设计（空/错/加载）、现代简洁风格与暗色模式规范时触发。
  能力：输出文字版 UI Spec（无需 Figma），定义信息层级、组件职责、交互细节与可访问性要点。
  边界：不写业务代码；规范必须可被 FE 直接实现并可验收。
  输出：UI Spec + 设计tokens（间距/圆角/字体/阴影/暗色模式）+ 关键交互与状态表。
---

# UX/UI Skill（文字版 UI Spec）

## 交付物
- UI Spec（文字版）：页面结构、组件清单、交互流程、状态设计
- 设计 tokens：间距（8px体系）、圆角、字体层级、阴影、暗色模式规则
- 可访问性要点：键盘操作、focus ring、aria 文案

## UI Spec 必含模块
- 顶部工具栏：搜索、过滤、排序、新建按钮、主题切换
- 任务列表：任务卡片（标题/优先级/负责人/dueDate/时效标签/完成状态）
- 任务编辑：Modal 或 Drawer（表单校验、Enter 提交、Esc 关闭）
- 删除确认：Modal（危险操作）
- 人员管理：Modal（新增/编辑/删除人员）
- 反馈：toast（新增/保存/删除/错误）

## 状态设计（必须）
- empty：无任务时的引导文案 + CTA
- loading：初始化读取 LocalStorage
- error：LocalStorage 解析失败/数据异常时提示并提供重置选项（可选）

## 输出格式
- 页面草图（文字）
- 组件清单（组件名→职责→props）
- 交互规则（点击/键盘/校验/反馈）

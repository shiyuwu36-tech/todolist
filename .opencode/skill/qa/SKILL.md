---
name: qa
description: |
  测试与质量闸门：把 PRD 的验收标准（A#）映射为可执行的测试用例矩阵，使用 Vitest 覆盖可测逻辑、用 Playwright 覆盖关键用户路径，并输出每迭代必跑的回归清单与复验命令。
  提供：A#→测试映射矩阵模板、Vitest/Playwright 测试样例与组织方式、回归清单（每迭代必测路径）、复验命令集合（lint/test/e2e），以及常见失败定位要点（断言/等待/选择器稳定性）。
  适用：需要把验收标准转为测试、补齐前端单测与端到端测试、覆盖任务 CRUD 与附件上传下载流程、验证重启后仍持久化（可手动+半自动）、在每次迭代结束形成质量闸门输出。
  不适用：实现业务功能本身、系统级安装与环境改造、引入复杂/重型测试框架或大规模性能压测（除非用户明确要求）。
---

# QA Skill（Vitest + Playwright）

## 测试策略
- Vitest：前端工具函数、apiClient 错误处理、表单校验逻辑（可测的部分）
- Playwright：关键用户路径（任务CRUD + 上传附件 + 重启后仍存在的验证步骤）
- 后端最小冒烟：可选（若不引入新框架，可在 e2e 里调用页面流程间接覆盖）

## 最小覆盖（Iteration 1）
- 1~2 个 Vitest：校验/错误处理
- 1 个 Playwright：创建任务并出现在列表（烟测）

## 后续覆盖建议（Iteration 2/3）
- Playwright：上传附件成功→列表出现→刷新仍在
- 持久化验证：重启后端后刷新页面→任务/附件仍存在（可作为手动验收 + 半自动化说明）

## 输出
1) 用例矩阵：A1…A? → 对应（Vitest/Playwright/手测）
2) 测试文件路径 + 完整代码
3) 回归清单：每次迭代结束必测路径
4) 复验命令：npm run lint / npm run test / npx playwright test
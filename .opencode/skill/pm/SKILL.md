---
name: pm
description: |
  触发场景：需要写 PRD、用户故事、验收标准、范围控制（MVP/可选）时触发。
  能力：把需求拆成可测试的验收条款（A1/A2…），明确边界与不做内容；输出 PRD 自检清单；需求变更先更新 PRD。
  边界：PRD 未通过前不得写代码；不得引入后端/登录/云同步等超范围能力。
  输出：中文 PRD（1-2页）+ 可测试验收标准 + 测试计划（与 QA 对齐）+ PRD 自检清单。
---

# PM Skill（PRD 与验收标准）

## 交付物
- PRD（1-2页）：目标用户、范围（MVP/可选）、用户故事、数据字段、非功能需求、测试计划
- 验收标准：每条必须可测试（手动步骤或自动化测试能验证）
- PRD 自检清单：逐条检查“可测/无歧义/不超范围”

## 规则
- PRD 未完成并自检通过前：禁止写任何代码
- 每个功能点必须对应验收标准编号（A1/A2…）
- 不清晰之处要写“假设 Assumptions”，并纳入 PRD

## 输出结构（固定）
1) 背景与目标
2) MVP 范围（必须做）/ 可选项（可不做）
3) 用户故事（US1…）
4) 数据模型字段（Task/Person）
5) 验收标准（A1…）
6) 非功能需求（性能/可访问性/兼容性/安全与隐私）
7) 测试计划（与 QA 对齐）
8) PRD 自检清单（勾选项）
